import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.joshlarminay.geovisualizer',
  appName: 'Geo Visualizer',
  webDir: 'dist',
};

export default config;
